/**
 * Enemy model manifest.
 *
 * Import this file once (e.g. in EnemyFactory.init) to self-register
 * every enemy type with EntityRegistry.
 *
 * To add a new enemy:
 *   1. Create  src/entities/models/<type>.js  following the existing pattern.
 *   2. Add     import './<type>.js'  below — that's the only change needed.
 */

import "./demon.js";
import "./zombie.js";
import "./ghost.js";
import "./brute.js";
